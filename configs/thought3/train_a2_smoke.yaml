schema_version: thought3.config.v1
variant: A2
experiment:
  name: thought3_train_a2_smoke
  output_dir: outputs/thought3/train/a2_smoke
runtime:
  backend: mock
  device: cpu
sampler:
  active_k: 2
data:
  dataset_revision: mock-phase-b-v1
  mock_sample_count: 16
cache:
  root: outputs/thought3/cache/smoke
  shard_size: 4
  pilot_limit: 16
training:
  max_steps: 10
  checkpoint_interval: 10
